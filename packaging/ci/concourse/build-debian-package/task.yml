---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: ubuntu
    tag: bionic

inputs:
- name: greenplum-database-release

outputs:
- name: debian-packages

PARAMS:
  PKG_NAME:

run:
  path: greenplum-database-release/packaging/ci/concourse/build-debian-package/run
